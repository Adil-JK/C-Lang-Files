#include<stdio.h>
#include<conio.h>
#include<stdlib.h>

main() {
	int per[10];
	int x,y;
	for(x=0; x<10; x++) {
		printf("Enter the value of index \n", x);	
		scanf("%d", &per[x]);
}
	for(y=0; y<10; y++) {
		printf("The value of index %d is %d \n", y, per[y]);
		
	}
	

}
